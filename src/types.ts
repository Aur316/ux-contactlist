export type ContactType = {
  id: number;
  name: string;
  email: string;
  phone: number;
};

export interface NameNumberProps {
  name: string;
  phone: number;
}

export interface ImageIconProps {
  src: string;
}
